# Component Inventory — Plugin FAQ (ut-plugin-faq)

## Core modules (`src/`)

- `src/main.go` — placeholder entry; registers navigation + loads locale bundle
- `src/ui` — FAQ page rendering
- `src/storage` — offline cache abstraction
- `src/manifest` — plugin manifest assets
- `src/faq` — FAQ content/model

## Tests

- `tests/unit`
- `tests/integration`

## Assets

- `assets/` — icons and localized content assets
