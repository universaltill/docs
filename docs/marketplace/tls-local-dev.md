# TLS for Local Development

Source: ut-market-place/docs/tls-local-development.md.

Notes:
- Marketplace server generates self-signed certs on first run and prompts to trust.
- Scripts: `./scripts/trust-dev-cert.sh`.
- Access Swagger at `https://localhost:8081/docs` after trusting.
